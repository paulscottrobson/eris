TODO
==== 
	EXISTS(file$)
	ALLOC(memsize)
	Convert to 8 colour RGB only.
	Update to 0.14
	Bully SPIFFS a bit
	Dog food ! Write a sprite editor.
	Timer function / Clock 
	Graphics system
		Rectangle, Line, Circle, Sprite functionality.
	Sprite system
	Sound system
	Inline assembler

Odd fixes/etc
=============

Kernel:
	Making the speaker work through the mouse port
	Recursion on keyboard queuing (don't expand if queue not zero)

Basic:
	Bodge single element else on its own :)

Emulator:
	Nothing outstanding


