TODO
==== 
	Fix program so it converts a named program e.g. parameters
	Dog food ! Design & Write a sprite editor.
	When it works upgrade to 0.2
	Bully SPIFFS a bit
	Timer function / Clock 
	Local ? Parameter ?
	Graphics system
		Rectangle, Line, Circle, Sprite functionality.
	Sprite system
	Sound system
	Inline assembler

Odd fixes/etc
=============

Kernel:
	Making the speaker work through the mouse port
	Recursion on keyboard queuing (don't expand if queue not zero)

Basic:
	Bodge single element else on its own :)

Emulator:
	Nothing outstanding


Sprite Editor :
	Arrow keys move about, Ctrl/Fire toggle pixel
	A-Z change selected sprite
	Ctrl+Q quits
	Ctrl+S save to clip
	Ctrl+L load from clip
	Ctrl+X/Y Flip